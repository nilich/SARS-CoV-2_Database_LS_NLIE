---
title: "SARS-CoV-2 Sequencing"
output: 
  flexdashboard::flex_dashboard:
    orientation: columns
    vertical_layout: fill
params:
     input: ""
    
---

Row
-----------------------------------------------------------------------

```{r, echo=F, warning=F}
#library(kableExtra)
#library(formattable)
#library(dplyr)
#library(flexdashboard)
library(knitr)
#library(shiny)
library(DT)
#args <- commandArgs(trailingOnly = TRUE)
setwd("/mnt/nfs/bio/Sequencing/Virology/SARS-CoV-2_Database_LS_NLIE")

file <- params$input

d <- read.csv(file, sep = ",", header=T)
 
 DT::datatable(d, rownames = F, class = "cell-border stripe",
   extensions = c("Buttons", "Select", "SearchPanes", "FixedColumns"),
   selection = "none",
   filter = "top",
   callback = JS("$.fn.dataTable.ext.errMode = 'none';"),
   options = list(
     autoWidth = T,
     dom = "Bfrt",
     pageLength = length(d),
     #columnDefs = list(
     #  list(width = "140px", targets = list(0, 1, 2, 3)),
     #  list(width = "140px", targets = list(4)),
     #  list(width = "140x", targets = list(5, 6, 7)),
     #  list(visible = FALSE, targets = list(10))
     #),
     searchHighlight = TRUE,
     buttons = c("csv", "excel", "pdf"),
     scrollX = 1000,
     scrollY = 500,
     fillContainer = TRUE,
     scroller = TRUE,
     # pageResize = T,
     deferRender = TRUE,
     scrollCollapse = T,
     callback = JS("$.fn.dataTable.ext.errMode = 'none';")
   ))

```

[View OpenGenomeBrowser](https://www.google.com) 